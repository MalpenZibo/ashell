#!/usr/bin/env bash
set -e
if [[ ! -d "/home/connerohnesorge/Documents/001Repos/ashell" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/connerohnesorge/Documents/001Repos/ashell")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/connerohnesorge/Documents/001Repos/ashell" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/connerohnesorge/Documents/001Repos/ashell/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/connerohnesorge/Documents/001Repos/ashell/.envrc" "/home/connerohnesorge/Documents/001Repos/ashell/.direnv"/*.rc
