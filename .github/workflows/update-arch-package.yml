name: Update Arch Package

on:
  workflow_call:
    inputs:
      plan:
        required: true
        type: string

jobs:
  update-arch-package:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: echo "Automatic update of the arch package is not yet implemented"
